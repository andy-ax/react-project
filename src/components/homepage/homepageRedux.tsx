import { Action } from 'redux';
import { connect } from 'react-redux';
import { HomepageUI } from './homepageUI';
import reduxController from '../../redux/redux-controller';

const homeReducer = (state: any, action: Action) => {
    
}